spring:
  application:
    name: petstorepetservice
  datasource:
    url: "jdbc:postgresql://postgreskirylsh.postgres.database.azure.com:5432/petstorepetservice_db?sslmode=require"
    username: "kiryladmin"
    password: "ja72_dsAW1o"
server:
  port: ${PETSTOREPETSERVICE_SERVER_PORT:8081}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
  show-actuator: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    ssl:
      enabled: false
    diskspace:
      enabled: false